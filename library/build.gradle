//apply plugin: 'com.android.application'
apply plugin: 'com.android.library'
apply plugin: 'com.github.dcendents.android-maven'
apply plugin: 'kotlin-android'
apply plugin: 'kotlin-android-extensions'
group = 'com.github.xuwennengcan'
android {
    compileSdkVersion defaultCompileSdkVersion

    defaultConfig {
        minSdkVersion defaultMinSdkVersion
        targetSdkVersion defaultTargetSdkVersion
        versionCode appVersionCode
        versionName appVersionName
        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }

    allprojects {
        repositories {
            maven { url "https://jitpack.io" }
            mavenCentral()
            maven { url 'https://maven.google.com' }
        }
    }

    sourceSets {
        main {
            java.srcDirs += rootProject.ext.kotlin_dir
        }
    }

    dependencies {
        implementation fileTree(dir: 'libs', include: ['*.jar'])
        testImplementation 'junit:junit:4.12'

        api dependents["appcompat-v7"]
        implementation dependents["recyclerview-v7"]
        implementation dependents["SpinKit"]
        implementation dependents["design"]
        implementation dependents["zxing"]
        implementation dependents["retrofit"]
        implementation dependents["gson"]
        implementation dependents["rxjava"]
        implementation dependents["rxandroid"]
        implementation dependents["eventbus"]
        api dependents["constraintLayout"]
        api dependents["kotlin"]
        api dependents["kotlin-coroutines"]

        api project(':resource')

    }
}

